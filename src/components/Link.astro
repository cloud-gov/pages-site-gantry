---
interface Props {
  href: string;
  className?: string;
  title?: string;
}
const { href, className, title } = Astro.props;
const base = import.meta.env.BASE_URL;
---

<a href={`${base}/${href}`} class={className} title={title}>
  <slot />
</a>
