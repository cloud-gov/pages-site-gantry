---
export interface Props {
  headingLevel?: 'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6';
  class?: string;
}

const {
  headingLevel = 'h2',
  class: customClass = '',
} = Astro.props;

const ValidHeadingLevels = ['h1', 'h2', 'h3', 'h4', 'h5', 'h6'];
const Tag = ValidHeadingLevels.includes(headingLevel) ? headingLevel : 'h2';
---

<Tag class={customClass}>
  <slot />
</Tag>